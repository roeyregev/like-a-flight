{
  "scripts": {
    "start": "node Backend/dist/app.js",
    "install": "cd Backend && npm install",
    "build": "cd Frontend && npm install && npm run build && cd ../Backend && npm install && npm run build && rm -rf ./dist/7-frontend && mv ../Frontend/build ./dist/7-frontend"
  },
  "engines": {
    "node": "18.x",
    "npm": "8.x"
  },
  "devDependencies": {
    "@types/node": "^22.13.1"
  }
}
